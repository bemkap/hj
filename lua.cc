#include"instance.hh"
#include"lua.hh"
/*
int translate(lua_State*L){
  instance*i=(instance*)lua_touserdata(L,1);
  double dx=lua_tonumber(L,2);
  double dy=lua_tonumber(L,3);
  i->x+=dx;
  i->y+=dy;
}
int rotate(lua_State*L){
  instance*i=(instance*)lua_touserdata(L,1);
  double da=lua_tonumber(L,2);
  
}
*/
